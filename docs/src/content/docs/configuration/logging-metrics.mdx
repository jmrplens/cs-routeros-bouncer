---
title: Logging & Metrics
description: Configure cs-routeros-bouncer logging (level and format) and the Prometheus metrics endpoint, including RouterOS system polling.
---

import { Badge, LinkCard } from "@astrojs/starlight/components";
import ConfigOption from "../../../components/ConfigOption.astro";

Settings for bouncer logging and Prometheus metrics.

## Logging

### `logging.level` <Badge text="Optional" variant="note" />

<ConfigOption path="logging.level" />

Log verbosity level:

| Level   | Description                                |
| ------- | ------------------------------------------ |
| `debug` | Detailed information for debugging (noisy) |
| `info`  | Normal operation messages                  |
| `warn`  | Warning conditions                         |
| `error` | Error conditions only                      |

```yaml
logging:
  level: "debug" # Useful for troubleshooting
```

### `logging.format` <Badge text="Optional" variant="note" />

<ConfigOption path="logging.format" />

Log output format:

- `text` — human-readable, colored output (best for console/journalctl)
- `json` — structured JSON (best for log aggregation systems)

### `logging.file` <Badge text="Optional" variant="note" />

<ConfigOption path="logging.file" />

Path to a log file. Leave it empty — the default — and the bouncer writes to stderr only, exactly as before this key did anything.

When set, log lines go to **both** stderr and the file. The file is an addition, not a redirection: `journalctl -u cs-routeros-bouncer` and `docker logs` keep working. The file is opened in append mode, so a restart never discards ban history, and created with mode `0640` because log lines record the IP addresses of banned clients.

:::caution[The parent directory must already exist]
The bouncer does not create directories. If the parent is missing, or the path is not writable, startup **fails** with the path and the OS error rather than falling back silently to stderr — a log file you configured and did not get is worse than no log file.

Any file path is accepted. Under the hardened systemd unit written by `setup`, `ReadWritePaths=/var/log` is the only writable location, so a path outside `/var/log` will fail even if it exists — and a relative path resolves against `/` there, not against your shell's working directory. Give an absolute path under `/var/log` for systemd deployments.
:::

There is no built-in rotation, and the file handle is held for the process lifetime. A rotation tool that renames the file (`logrotate` without `copytruncate`) will leave the bouncer writing to the renamed inode until it restarts — use `copytruncate`, or rely on `journalctl` and leave this key unset.

## Prometheus Metrics

### `metrics.enabled` <Badge text="Optional" variant="note" />

<ConfigOption path="metrics.enabled" />

Enable the Prometheus metrics endpoint. When enabled, the bouncer exposes metrics at `/metrics`. The `/health` health check is always served on the metrics listener regardless of this setting.

### `metrics.listen_addr` <Badge text="Optional" variant="note" />

<ConfigOption path="metrics.listen_addr" />

Listen address for the metrics HTTP server.

### `metrics.listen_port` <Badge text="Optional" variant="note" />

<ConfigOption path="metrics.listen_port" />

Listen port for the metrics HTTP server.

```yaml
metrics:
  enabled: true
  listen_addr: "0.0.0.0"
  listen_port: 2112
```

### `metrics.routeros_poll_interval` <Badge text="Optional" variant="note" />

<ConfigOption path="metrics.routeros_poll_interval" />

How often to poll RouterOS for system metrics (CPU load, memory, temperature). Set to `0` to disable system metrics collection.

```yaml
metrics:
  routeros_poll_interval: "30s"
```

### `metrics.track_processed` <Badge text="Advanced" variant="caution" />

<ConfigOption path="metrics.track_processed" />

Enable tracking of processed (non-blocked) traffic through the firewall. When enabled, the bouncer creates passthrough counting rules on RouterOS to measure all traffic passing through monitored chains and reports the data to both Prometheus and LAPI.

Set to `false` to disable processed traffic tracking, which avoids creating additional rules on the router and reduces metric overhead.

```yaml
metrics:
  track_processed: true
```

### Available metrics

<LinkCard
  title="Prometheus Metrics Reference"
  description="Full list of all exported metrics with PromQL examples"
  href="/cs-routeros-bouncer/monitoring/prometheus/"
/>

:::caution[Security]
The metrics endpoint should not be exposed to the internet. Use firewall rules or network segmentation to restrict access.
:::
