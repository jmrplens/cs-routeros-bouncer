---
title: Visión general de la configuración
description: "Todas las opciones de configuración de cs-routeros-bouncer de un vistazo: claves YAML, variables de entorno, valores por defecto y referencias detalladas."
slug: es/configuration
---

import { LinkCard, CardGrid } from "@astrojs/starlight/components";
import ConfigOption from "../../../../components/ConfigOption.astro";

cs-routeros-bouncer se puede configurar mediante un **archivo YAML** y/o **variables de entorno**. Las variables de entorno tienen prioridad sobre los valores del archivo de configuración.

## Archivo de configuración

Cuando ejecutas el binario directamente, pasa la ruta de configuración con `-c`, salvo que todos los ajustes obligatorios se proporcionen mediante variables de entorno:

```bash
cs-routeros-bouncer -c /path/to/config.yaml
```

Los asistentes de despliegue eligen sus propias rutas:

| Método de despliegue          | Ruta de configuración usada                                                                                    |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Servicio systemd `setup`      | `/etc/cs-routeros-bouncer/cs-routeros-bouncer.yaml`                                                            |
| Imagen Docker                 | Variables de entorno por defecto; el `/etc/cs-routeros-bouncer/config.yaml` implícito se carga si está montado |
| Ejecución directa del binario | `-c` cuando se indica; en caso contrario `/etc/cs-routeros-bouncer/config.yaml` si ese archivo existe          |

La carga implícita de `/etc/cs-routeros-bouncer/config.yaml` es una propiedad del binario, no de la imagen Docker: cualquier ejecución sin `-c` carga ese archivo cuando existe y, si no, funciona solo con variables de entorno.

Los valores de cadena YAML pueden contener marcadores explícitos `${VAR}`. Solo se expanden los marcadores con llaves; los caracteres `$` literales de los secretos se conservan.

En el repositorio se incluye una referencia completa comentada en [`config/cs-routeros-bouncer.yaml`](https://github.com/jmrplens/cs-routeros-bouncer/blob/main/config/cs-routeros-bouncer.yaml).

## Secciones de configuración

<CardGrid>
  <LinkCard
    title="MikroTik"
    description="Conexión a la API de RouterOS, TLS y tiempos de espera."
    href="/cs-routeros-bouncer/es/configuration/mikrotik/"
  />
  <LinkCard
    title="CrowdSec"
    description="Conexión a LAPI, sondeo y filtrado de decisiones."
    href="/cs-routeros-bouncer/es/configuration/crowdsec/"
  />
  <LinkCard
    title="Blocklists de CAPI"
    description="Blocklists comunitarias, modo solo local y recomendaciones de escala."
    href="/cs-routeros-bouncer/es/configuration/capi-blocklists/"
  />
  <LinkCard
    title="Firewall"
    description="IPv4/IPv6, reglas filter/raw, bloqueo de salida y registro."
    href="/cs-routeros-bouncer/es/configuration/firewall/"
  />
  <LinkCard
    title="Registro y métricas"
    description="Nivel/formato de log y endpoint de métricas de Prometheus."
    href="/cs-routeros-bouncer/es/configuration/logging-metrics/"
  />
  <LinkCard
    title="Ajuste de rendimiento"
    description="Tamaño del pool, operaciones en bloque, tiempos de espera y cadencia de reconciliación."
    href="/cs-routeros-bouncer/es/configuration/performance-tuning/"
  />
  <LinkCard
    title="Ejemplos"
    description="Ejemplos de configuración completos para escenarios habituales."
    href="/cs-routeros-bouncer/es/configuration/examples/"
  />
</CardGrid>

## Referencia rápida

Todas las opciones de un vistazo. Consulta las páginas dedicadas para las descripciones detalladas.

### Parámetros básicos

Los ajustes esenciales para poner en marcha el bouncer. La mayoría de los despliegues solo necesitan estos.

<ConfigOption
  rows={[
    ["crowdsec.api_url", "URL de la LAPI de CrowdSec"],
    ["crowdsec.api_key", "API key del bouncer"],
    ["mikrotik.address", "Dirección de la API de RouterOS (`host:port`)"],
    ["mikrotik.username", "Usuario de la API"],
    ["mikrotik.password", "Contraseña de la API"],
    ["firewall.ipv4.enabled", "Habilitar el bloqueo IPv4"],
    ["firewall.ipv6.enabled", "Habilitar el bloqueo IPv6"],
    ["firewall.filter.enabled", "Crear reglas de firewall filter"],
    ["firewall.raw.enabled", "Crear reglas raw/prerouting"],
    ["firewall.deny_action", "Acción: `drop` o `reject`"],
    ["logging.level", "Nivel de log: `debug`, `info`, `warn`, `error`"],
  ]}
/>

`mikrotik.address` y `mikrotik.username` no tienen valor por defecto. Los valores `192.168.0.1:8728` y `crowdsec` que aparecen en `config/cs-routeros-bouncer.yaml` son valores de ejemplo, no valores por defecto.

### Parámetros avanzados

Opciones de ajuste fino para el filtrado de decisiones, TLS, rendimiento, personalización del firewall y observabilidad. Los valores por defecto (sondeo de 10 s, reconciliación de 15 m, `pool_size: 4`) están dimensionados para un único router con hasta unas 30.000 decisiones activas; ajústalos solo por encima de esa escala o en hardware modesto.

#### [CrowdSec](/cs-routeros-bouncer/es/configuration/crowdsec/) — sondeo, filtrado y TLS

<ConfigOption
  rows={[
    ["crowdsec.update_frequency", "Intervalo de sondeo"],
    [
      "crowdsec.reconciliation_interval",
      "Reconciliación periódica de address-list (`0` = deshabilitada, mínimo `1m`)",
    ],
    [
      "crowdsec.lapi_metrics_interval",
      "Intervalo de envío de métricas de uso (`0` = deshabilitado)",
    ],
    ["crowdsec.origins", "Filtrar por origen (vacío = todos los orígenes)"],
    ["crowdsec.scopes", "Ámbitos de decisión"],
    [
      "crowdsec.supported_decisions_types",
      "Tipos de decisión (solo `ban` está implementado)",
    ],
    [
      "crowdsec.scenarios_containing",
      "Incluir solo los escenarios coincidentes",
    ],
    [
      "crowdsec.scenarios_not_containing",
      "Excluir los escenarios coincidentes",
    ],
    ["crowdsec.retry_initial_connect", "Reintentar en el arranque"],
    ["crowdsec.insecure_skip_verify", "Omitir la verificación TLS"],
    ["crowdsec.cert_path", "Ruta del certificado de cliente"],
    ["crowdsec.key_path", "Ruta de la clave de cliente"],
    ["crowdsec.ca_cert_path", "Ruta del certificado de la CA"],
  ]}
/>

`CROWDSEC_ORIGINS` se separa con espacios cuando se define como variable de entorno, por ejemplo `CROWDSEC_ORIGINS="crowdsec cscli"`.

#### [MikroTik](/cs-routeros-bouncer/es/configuration/mikrotik/) — TLS y rendimiento

<ConfigOption
  rows={[
    ["mikrotik.tls", "Usar TLS"],
    ["mikrotik.tls_insecure", "Omitir la verificación TLS"],
    ["mikrotik.connection_timeout", "Tiempo de espera de conexión"],
    ["mikrotik.command_timeout", "Tiempo de espera de comando"],
    ["mikrotik.pool_size", "Conexiones API en paralelo (1–20)"],
  ]}
/>

#### [Firewall](/cs-routeros-bouncer/es/configuration/firewall/) — reglas, interfaces y registro

<ConfigOption
  rows={[
    ["firewall.ipv4.address_list", "Nombre de la lista IPv4"],
    ["firewall.ipv6.address_list", "Nombre de la lista IPv6"],
    [
      "firewall.filter.chains",
      "Cadenas filter; en los valores de entorno separados por comas se recorta el espacio en blanco alrededor de cada entrada",
    ],
    [
      "firewall.raw.chains",
      "Cadenas raw; en los valores de entorno separados por comas se recorta el espacio en blanco alrededor de cada entrada",
    ],
    [
      "firewall.rule_placement",
      "Forma abreviada de cadena simple para la colocación global",
    ],
    [
      "firewall.rule_placement.strategy",
      "Estrategia en forma de objeto: `top`, `bottom`, `position`, `before_comment` o `after_comment`",
    ],
    [
      "firewall.rule_placement.comment",
      "Comentario ancla para la colocación por comentario",
    ],
    [
      "firewall.rule_placement.comment_match",
      "Modo de coincidencia del comentario: `exact` o `contains`",
    ],
    [
      "firewall.rule_placement.position",
      "Posición `print` de RouterOS (base cero) obligatoria cuando la estrategia es `position`",
    ],
    [
      "firewall.rule_placement.fallback",
      "Alternativa para la colocación por comentario: `top` o `bottom`",
    ],
    [
      "firewall.rule_placement.filter",
      "Sobrescritura para la tabla filter; hereda los campos no especificados de la colocación global",
    ],
    [
      "firewall.rule_placement.raw",
      "Sobrescritura para la tabla raw; hereda los campos no especificados de la colocación global",
    ],
    [
      "firewall.ipv4.rule_placement",
      "Sobrescritura de colocación solo IPv4; hereda los campos no especificados de la colocación global",
    ],
    [
      "firewall.ipv4.rule_placement.filter",
      "Sobrescritura IPv4 para la tabla filter; hereda de la colocación global y de la IPv4",
    ],
    [
      "firewall.ipv4.rule_placement.raw",
      "Sobrescritura IPv4 para la tabla raw; hereda de la colocación global y de la IPv4",
    ],
    [
      "firewall.ipv6.rule_placement",
      "Sobrescritura de colocación solo IPv6; hereda los campos no especificados de la colocación global",
    ],
    [
      "firewall.ipv6.rule_placement.filter",
      "Sobrescritura IPv6 para la tabla filter; hereda de la colocación global y de la IPv6",
    ],
    [
      "firewall.ipv6.rule_placement.raw",
      "Sobrescritura IPv6 para la tabla raw; hereda de la colocación global y de la IPv6",
    ],
    ["firewall.comment_prefix", "Prefijo de comentario"],
    ["firewall.log", "Habilitar el registro de reglas"],
    ["firewall.log_prefix", "Prefijo de log global"],
    [
      "firewall.reject_with",
      "Tipo de rechazo cuando `deny_action=reject`: `icmp-network-unreachable`, `icmp-host-unreachable`, `icmp-port-unreachable`, `icmp-protocol-unreachable`, `icmp-network-prohibited`, `icmp-host-prohibited`, `icmp-admin-prohibited`, `tcp-reset`",
    ],
    [
      "firewall.filter.log_prefix",
      "Sobrescribir el prefijo de log para las reglas filter",
    ],
    [
      "firewall.filter.connection_state",
      "Estados separados por comas: `new`, `established`, `related`, `invalid`, `untracked`; se recorta el espacio en blanco alrededor de las entradas; solo minúsculas, sin negación",
    ],
    [
      "firewall.raw.log_prefix",
      "Sobrescribir el prefijo de log para las reglas raw",
    ],
    [
      "firewall.block_input.interface",
      "Restringir las reglas input/raw a una interfaz (vacío = todas)",
    ],
    [
      "firewall.block_input.interface_list",
      "Restringir las reglas input/raw a una lista de interfaces (vacío = todas)",
    ],
    [
      "firewall.block_input.whitelist",
      "Address-list para la lista blanca de entrada (accept antes de drop)",
    ],
    ["firewall.block_output.enabled", "Bloquear el tráfico saliente"],
    ["firewall.block_output.interface", "Interfaz WAN"],
    ["firewall.block_output.interface_list", "Lista de interfaces WAN"],
    [
      "firewall.block_output.log_prefix",
      "Sobrescribir el prefijo de log para las reglas de salida",
    ],
    [
      "firewall.block_output.passthrough_v4",
      "Cliente IPv4 que omite el bloqueo de salida",
    ],
    [
      "firewall.block_output.passthrough_v4_list",
      "Lista IPv4 que omite el bloqueo de salida",
    ],
    [
      "firewall.block_output.passthrough_v6",
      "Cliente IPv6 que omite el bloqueo de salida",
    ],
    [
      "firewall.block_output.passthrough_v6_list",
      "Lista IPv6 que omite el bloqueo de salida",
    ],
  ]}
/>

Los alias retrocompatibles como `FIREWALL_INPUT_*` y `FIREWALL_OUTPUT_*` se siguen aceptando para despliegues antiguos. Prefiere los nombres explícitos `FIREWALL_BLOCK_INPUT_*` y `FIREWALL_BLOCK_OUTPUT_*` en las configuraciones nuevas.

#### [Registro y métricas](/cs-routeros-bouncer/es/configuration/logging-metrics/) — formato, salida a archivo y Prometheus

<ConfigOption
  rows={[
    ["logging.format", "Formato de log: `text` o `json`"],
    [
      "logging.file",
      "Escribe también los logs en este archivo; stderr se mantiene siempre",
    ],
    ["metrics.enabled", "Habilitar el endpoint `/metrics` de Prometheus"],
    ["metrics.listen_addr", "Dirección de escucha"],
    ["metrics.listen_port", "Puerto de escucha"],
    [
      "metrics.routeros_poll_interval",
      "Intervalo de sondeo de métricas del sistema RouterOS (0 para deshabilitar)",
    ],
    [
      "metrics.track_processed",
      "Crear reglas passthrough de conteo para las métricas de tráfico procesado",
    ],
  ]}
/>
